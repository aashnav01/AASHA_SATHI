# Render (and any host that sets NODE_ENV=production) makes npm omit
# devDependencies, but the build tools - vite, typescript, @types/node - all
# live there. Install them regardless; they are build-time only and are not
# bundled into the deployed runtime.
include=dev
